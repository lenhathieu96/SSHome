import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  radioContainer: {
    flexDirection: 'row',
    padding: 10,
    alignItems: 'center',
  },
  textStyle: {
    marginHorizontal: 10,
  },
});

export default styles;
